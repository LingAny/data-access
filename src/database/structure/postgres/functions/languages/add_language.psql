CREATE OR REPLACE FUNCTION add_language(
  p_language_id UUID,
  p_title TEXT)

RETURNS void
AS
$BODY$
  BEGIN
    INSERT INTO languages (language_id, title)
    VALUES (p_language_id, p_title);
  END;
$BODY$
LANGUAGE plpgsql VOLATILE;
